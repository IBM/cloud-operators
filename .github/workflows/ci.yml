name: CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: CI
    runs-on: ubuntu-24.04
    timeout-minutes: 45

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.17.13
          cache: true

      - name: Lint
        run: make lint

      - name: Run unit tests
        run: make test-unit

      - name: Validate release artifacts
        run: make validate-release RELEASE_VERSION=1000.0.0

# missing:
# - e2e (was disabled in Travis)
# - int-test (called coverage-unit in the Makefile and unit-tests in Travis) (cannot run on forks, is this needed even?)
# - release
