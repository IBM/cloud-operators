apiVersion: ibmcloud.ibm.com/v1beta1
kind: Binding
metadata:
  name: binding-translator
spec:
  serviceName: mytranslator
  secretName: translator-secret
  parameters:
  - name: serviceid_crn
    value: crn:v1:bluemix:public:iam-identity::a/ACCOUNT_ID::serviceid:SERVICE_ID
